
import { formataData } from 'agenda/helpers/formata-data';
import { module, test } from 'qunit';

module('Unit | Helper | formata data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formataData([42]);
  assert.ok(result);
});

